distribution/out/

# Generated by the book and paper builds. web/downloads/ carries the published
# copies, so this is redundant in the repo. It is already tracked from an
# earlier commit, so this line only takes effect after:
#   git rm -r --cached build
build/
.vercel
.env*
